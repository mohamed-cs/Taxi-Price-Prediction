<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.taxiprediction">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaxiPrediction">
        <activity android:name=".yossef"></activity>
        <activity android:name=".leadership" />
        <activity android:name=".dataactivity2" />
        <activity android:name=".dataactivity" />
        <activity android:name=".chooseactivity" />
        <activity android:name=".splashimages2" />
        <activity android:name=".splashimages1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>